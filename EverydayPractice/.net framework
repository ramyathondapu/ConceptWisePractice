.net framework is a library which supports for devolopers to devolope web based applications it allows object oriented programm to use for scripting 
intermediate language this is the language that generated from source code but can not directly executed  by CPU ,  this can execute on any computer any environment if that machine has JVM
intermediate code generated by CLR
managed code is the code mangaed at run time, which is executed by CLR while unmanaged code is exceuted by OS , un managed is unsafe which will create security threats
while managed code is secured, un manage code gives low level of access while managed code does not
just in time compiler is used to generate machine level code from managed code at run time
framework class library is having multiple classes functions and datatypes which are used to perform diffrent operations and to buold desktop and web bsed applications